@model LostMonitors.Web.Models.GamesModel

<div id="selection">
    <select id="player1"> @foreach (var item in Model.AvailablePlayers) { <option>@item.Name</option> } </select> vs.
    <select id="player2"> @foreach (var item in Model.AvailablePlayers) { <option>@item.Name</option> } </select>
    <button id="start">Start</button>
</div>

<div id="board">
    <button id="turn">Turn</button>
</div>

<div id="board">
    
</div>

@section Scripts{
    @Scripts.Render("~/bundles/app")
    @Scripts.Render("~/signalr/hubs")
}